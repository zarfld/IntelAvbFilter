# Runtime data directory — contains Google auth tokens, session state, and user-specific
# notebook library config. NEVER commit — personal credentials and local state only.
data/

# Python virtual environment — binaries, not source; recreated via pip
.venv/
